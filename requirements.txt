# Developer Control Plane — core dependencies (Tier 0, fully offline)
watchdog>=6.0  # <6 crashes on Python 3.13 (threading handle change)
fastapi>=0.110
uvicorn>=0.29
pydantic>=2.6
rich>=13.0
pyyaml>=6.0

# Testing
pytest>=8.0
httpx>=0.27
